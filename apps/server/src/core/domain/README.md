# 领域模型目录

## 说明

此目录用于存放领域模型和领域逻辑，遵循DDD（领域驱动设计）思想。

## 目录结构

- `entities/`: 领域实体
- `value-objects/`: 值对象
- `aggregates/`: 聚合根
- `domain-events/`: 领域事件

## 设计原则

1. **领域驱动**：以业务领域为核心
2. **封装**：领域逻辑封装在领域对象内部
3. **无副作用**：领域模型不包含IO操作
