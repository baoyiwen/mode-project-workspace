# 核心模型目录

## 说明

此目录用于存放数据模型和实体类，这些类应该是纯数据模型，不包含任何IO操作或副作用。

## 目录结构

- `entities/`: 实体类定义
- `value-objects/`: 值对象定义
- `dto/`: 数据传输对象

## 设计原则

1. **纯数据模型**：不包含业务逻辑
2. **不可变性**：尽量使用不可变数据结构
3. **类型安全**：充分利用TypeScript类型系统
