/**
 * 核心层统一导出
 * 纯规范与领域模型（无IO）
 */

// 导出类型定义
export * from './types';

// 导出模型（如果有）
export * from './model';

// 导出服务（如果有）
// export * from './service';

// 导出领域模型（如果有）
// export * from './domain';
